# Supabase — PSA PETS backend (publishable key is browser-safe; RLS is insert-only).
# These values are also bundled as fallbacks in src/lib/supabase.ts, so the
# static build works without this file. Override here for staging/local.
VITE_SUPABASE_URL=https://eutszmrsukoqqeilzrbv.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_TUh1vZatXhOirPVOgcllYQ_HC9DCdYr
